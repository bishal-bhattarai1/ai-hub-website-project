# AI Hub Frontend

This folder is a static frontend export for Vercel.

Deploy settings:

- Framework Preset: Other
- Root Directory: `frontend`
- Build Command: empty or `npm run build`
- Output Directory: `.`
- Install Command: empty

This frontend does not require Django to run. Django admin, database forms, authentication, and content management still live in the main Django project.

